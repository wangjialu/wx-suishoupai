<view class="container">
    <view class="article-name">{{articleInfo.title}}</view>
    <view class="article-time">{{articleInfo.createTimeStr}}</view>
    <view class="article-content">
        <rich-text nodes="{{articleInfo.content}}" space="nbsp"></rich-text>
    </view>
</view>
